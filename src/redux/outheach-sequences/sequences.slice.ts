import { createSlice } from "@reduxjs/toolkit";
import { sequencesStoreKey } from "./sequences.const";


export const sequencesSlice = createSlice({
    name: sequencesStoreKey,
    initialState:
    reducers:{
      
    }
})